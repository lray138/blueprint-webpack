<%- require("/blueprint/templates/default.ejs")({
    head: {
        title: "Blog",
    },
    content: require("/blueprint/partials/wraps/section-one-col.ejs")({
            attrs: {
                section: {
                    class_add: "mt-5",
                }
            },
            content: require("/blueprint/partials/patterns/page-header/index.ejs")({
                heading: "Blog",
                text: "A collection of thoughts and musings."
            })
            +
            require("/blueprint/partials/components/list/ul.ejs")({
                list_items: [
                    {
                        class_names: "test-again",
                        content: "test",
                    }
                ]
            })
        }) 
}) %>