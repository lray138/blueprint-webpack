<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Kvm | lray138fp | Blueprint Docs</title>
    <script src="https://cdn.jsdelivr.net/gh/gnat/css-scope-inline@main/script.js"></script>
  </head>
  <body class="d-flex flex-column min-vh-100">
    <div class="container-fluid">
      <%- require("/blueprint/partials/header.ejs")({ base_url }) %>
    </div>

    <section>
      <div class="container-fluid">
        <div class="row">
          <%- require("/blueprint/partials/docs/sidebar.ejs")() %>

          <div class="col-12 col-lg-6 col-xl-8 py-7 py-lg-9 px-lg-7" id="doc-content">
            <h1 class="mb-2">Kvm</h1>
            <p class="fs-lg text-muted mb-4">Key/value map helper.</p>

            <h2>Import</h2>
            <pre class="mb-4 bg-light p-3 rounded"><code class="language-js">const fp = require("/blueprint/js/lray138fp.min.js");
const { Kvm } = fp;</code></pre>

            <h2>Methods</h2>

            <h3>of()</h3>
            <p>Create a Kvm from an object.</p>

            <h3>tryPath(path)</h3>
            <p>Safe lookup. Returns a Result (Ok/Err) so you can <code>getOrElse</code>.</p>
            <pre class="mb-4 bg-light p-3 rounded"><code class="language-js">Kvm.of({ a: { b: 1 } })
  .tryPath("a.b")
  .getOrElse(0)
  .get();</code></pre>

            <h3>path(path)</h3>
            <p>Unsafe lookup (throws/Err on missing paths depending on implementation).</p>
          </div>

          <%- require("/blueprint/partials/docs/jump-to.ejs")() %>
        </div>
      </div>
    </section>

    <div class="container mt-auto">
      <%- require("/blueprint/partials/footer.ejs")() %>
    </div>
    <%- require("/blueprint/partials/svg-icons.ejs")() %>
  </body>
</html>
