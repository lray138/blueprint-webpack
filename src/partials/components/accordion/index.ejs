<% 
    var base_class = "accordion";
    var modifiers = typeof flush !== 'undefined' && flush ? "accordion-flush" : "";

    var items = items
        .map(item => require('/partials/components/accordion/item.ejs')(item))
        .join('');

    var classNames = base_class + " " + modifiers;
    var id = typeof id !== 'undefined' && id ? id : "accordionExample";
%>
<div class="<%= classNames %>" id="<%= id %>" <%- __attributes__ %>>
    <%- items %>    
</div>

<!-- 
// accordion component 
// id - unique id generated by PHP or JS, required 
// base_class: accordion - hidden
// modifiers: accordion-flush - multiselect

// accordion component item 
// button_text: Str example:"Accordion Item #1",
// target_id: Str "collapseOne",
// parent_id: Str "accordionExample",
// content:  Str 
